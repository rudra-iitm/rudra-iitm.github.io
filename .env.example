# Environment Variables
# Since this project is statically exported for GitHub Pages, 
# only variables prefixed with VITE_ will be available to the client at build time.

# Example:
# VITE_API_URL=https://api.example.com
